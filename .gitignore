# Ignore cached files
qa-cache/*/

# Ignore backed up SSL certs
local_certs

# Also ignore the install script, once it's been moved
config/qa-install.php

# This is a premium plugin; it is not public code, so don't track it
pupi-dm

# Other files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.idea/
dbdata
.vscode
*.pem
*.ppk
vendor
google-auth.json
Dockerfile_bob
*local-compose.yml

# Anything in this folder will not be tracked, obviously
untracked/
